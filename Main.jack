class Main
{
   function void main()
   {
      var ZombieGame game;

      while (true)
      {
         do Screen.clearScreen();
         do Sys.wait(250);
         let game = ZombieGame.new();
         do game.run();
         do game.dispose();
      }

      return; 
   }
}